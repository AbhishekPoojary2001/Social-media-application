<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-between">
            <a class="text-emerald-700"href="/profile">Go back</a>
            <a class="bg-red-500 py-2 px-3 rounded-md text-sm mb-5 inline-block" href="/logout">logout</a>
        </div>
        <h4 class="text-xl mb-5 text-blue-500">Edit post</h4>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="Make changes" class="block w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"name="content"><%= post.content %></textarea>
            <input class="w-[24%] block bg-orange-500 font-semibold py-2 rounded-md mt-4" type="submit" value="Update Post">
        </form>
        
    </div>
</body>
</html>